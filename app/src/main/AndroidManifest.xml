<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mcw0805.wheres_my_stuff">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Controller.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Controller.RegistrationActivity" />
        <activity android:name=".Controller.LogInActivity" />
        <activity android:name=".Controller.ProfileActivity" />
        <activity android:name=".Controller.Dashboard" />
        <activity android:name=".Controller.LostItemFormActivity" />
        <activity android:name=".Controller.ResetPasswordActivity" />
        <activity android:name=".Controller.LostItemListActivity" />
        <activity android:name=".Controller.ListView" />
        <activity android:name=".Controller.ItemViewerActivity"></activity>
    </application>

</manifest>